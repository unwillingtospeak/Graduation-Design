import type { DataTableColumnSource, DataTableColumnInternal, DataTableItem, DataTableSortingOptions } from '../types';
interface useColumnsProps {
    columns: DataTableColumnSource[];
    items: DataTableItem[];
    sortingOptions: DataTableSortingOptions;
    [prop: string]: unknown;
}
export declare const sortingOptionsValidator: (options: DataTableSortingOptions) => boolean;
export declare const buildTableColumn: (source: DataTableColumnSource, initialIndex: number, props: useColumnsProps) => DataTableColumnInternal;
export default function useColumns(props: useColumnsProps): {
    columnsComputed: import("vue").ComputedRef<DataTableColumnInternal[]>;
};
export {};
