import { getCurrentInstance, shallowRef, onMounted, onUpdated, onBeforeUnmount } from "vue";
const useCurrentElement = (el) => {
  if (el) {
    return el;
  }
  const vm = getCurrentInstance();
  const currentEl = shallowRef();
  onMounted(() => {
    var _a;
    currentEl.value = (_a = vm.proxy.$el) != null ? _a : void 0;
  });
  onUpdated(() => {
    var _a;
    currentEl.value = (_a = vm.proxy.$el) != null ? _a : void 0;
  });
  onBeforeUnmount(() => {
    var _a;
    currentEl.value = (_a = vm.proxy.$el) != null ? _a : void 0;
  });
  return currentEl;
};
export { useCurrentElement as u };
//# sourceMappingURL=useCurrentElement.js.map
