import { c as createGlobalConfig, G as GLOBAL_CONFIG } from "../global-config.js";
import { d as defineGlobalProperty } from "../../../vuestic-plugin/utils/global-properties.js";
import { d as defineVuesticPlugin } from "../../../vuestic-plugin/types/index.js";
const GlobalConfigPlugin = defineVuesticPlugin((config) => ({
  install(app) {
    const globalConfig = createGlobalConfig();
    if (config) {
      globalConfig.mergeGlobalConfig(config);
    }
    app.provide(GLOBAL_CONFIG, globalConfig);
    defineGlobalProperty(app, "$vaConfig", globalConfig);
  }
}));
export { GlobalConfigPlugin as G };
//# sourceMappingURL=index.js.map
