const validateSlider = (value, step, min, max, range) => {
  const inRange = (v) => {
  };
  if (Array.isArray(value)) {
    value.map(inRange);
  }
  return true;
};
export { validateSlider as v };
//# sourceMappingURL=validateSlider.js.map
