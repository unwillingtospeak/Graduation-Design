import { getCurrentInstance, provide, toRef, inject, ref } from "vue";
const key = Symbol("VaSidebar");
const useSidebar = () => {
  const { props } = getCurrentInstance();
  provide(key, {
    color: toRef(props, "color")
  });
};
const useSidebarItem = () => {
  const { color } = inject(key, { color: ref("white") });
  return {
    sidebarColor: color
  };
};
export { useSidebarItem as a, useSidebar as u };
//# sourceMappingURL=useSidebar.js.map
